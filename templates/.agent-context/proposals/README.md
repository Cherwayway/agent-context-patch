# Proposals

Store context evolution proposals here.

Use:

```text
YYYY-MM-DD-short-slug.md
```

If there are more than 10 pending proposals, run `$evolve review-context`
before creating new proposals.


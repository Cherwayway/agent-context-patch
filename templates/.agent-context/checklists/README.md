# Checklists

This directory starts without active domain checklists.

$evolve init detects domain candidates, shows one reviewable InitPlan, and
materializes a checklist only after that domain is approved and added to
config.yml enabled_domains.

config.yml is the only truth source for enabled domain packs. A
workspace-specific checklist may also be created by an approved proposal when
no reusable pack fits.

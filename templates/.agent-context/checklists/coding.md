# Coding Checklist

- Read active project guidance before editing.
- Confirm the requirement source when implementation scope is non-trivial.
- Keep edits scoped to the requested behavior.
- Run the narrowest useful validation.
- Report validation results.
- If a mistake is reusable, create an `$evolve after-failure` proposal.


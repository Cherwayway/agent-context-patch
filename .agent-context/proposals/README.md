# Proposals

Store only valid Workspace Schema v1 proposal aggregates here. Product-source
changes to the kit itself are tracked through issues, pull requests, tests, and
immutable Releases; do not encode a second kit-level proposal lifecycle here.
